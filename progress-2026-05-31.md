# Progress — 2026-05-31

## Done this session

### Hcg tag (300 occurrences) — Mapped
- **mapper.rs**: `"Hcg"` arm reads attr `"0"` (file number), prepends `"hcg_"`, maps transition via `map_event_transition(attr "1")`
- Emits `[Window{off}, HideFg{all}, ShowCg{hcg_NNN, transition}]` — same pattern as `Event`/`EventMN`/`EventCut`
- **Zero changes to script.rs / script_runner.rs / rendering.rs** — reuses existing `ShowCg` machinery
- Gallery already has Safe Mode awareness of `hcg_*` filenames
- Lua behavior preserved: `msgoff` (→ `Window{off}`), `reset_fg()` (→ `HideFg{all}`), then `DrawScreen` with forced `"hcg_"` prefix
- **3 new tests** (`test_map_hcg`, `test_map_hcg_default_transition`, `test_map_hcg_sudden`)

### Ending_Base tag (6 occurrences) — Mapped to existing commands
- **mapper.rs**: `"Ending_Base"` arm reads attr `"0"` (condition expression like `"t.tmp==1"`) — not used at mapping time, condition evaluation is a runtime concern
- Emits `[Window{off}, GameMode{mode:2}, Wait{duration:5000}]`:
  - `Window { show: false }` — closes text window (matches Lua's `btnoff()`)
  - `GameMode { mode: 2 }` — sets `settings.click_to_advance = true` for ending mode
  - `Wait { duration: 5000 }` — 5s pause simulating credits duration
- ASB context preserved: `GetGlobalFlag → Ending_Base → SetGlobalFlag → RouteFlag → Blackout` — the subsequent commands (already mapped) continue after `Wait` expires or user clicks through
- **1 new test** (`test_map_ending_base`)

## Test counts
- **80/80 exporter tests pass** (was 76 → 79 with Hcg → 80 with Ending_Base)
- `cargo check` (main crate) clean

## Now-mapped ASB tags
`sel_init`, `sel_text`, `select`, `Select`, `exswitch`, `SetLocalFlag`, `GetLocalFlag`, `LoadValueFromLocalWork`, `StoreValueToLocalWork` (with expression eval), `GetGlobalFlag`, `SavePoint`, `Refresh`, `SetGlobalFlag`, `NextDay`, `Jishin`, `SetValidityOfSaving`, `SetValidityOfLoading`, `SetValidityOfInput`, **`Hcg`**, **`Ending_Base`**

## Remaining unmapped ASB tags (>5 occurrences)
`Size` (206), `exif` (79), `sys_trans` (17), `lyc2` (5), `GetExecutionMode`

## Commits
- `27d3db848` feat: map Hcg tag
- `05186251d` feat: map Ending_Base tag
